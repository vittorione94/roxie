# MPO arm of the CMU_006_13 overfit sweep: Gaussian policy, single Q critic,
# EM-style update with KL-constrained E/M steps and learned duals. Explores from
# its own policy, so its launchable carries NO noise group.
#
# SWEEP INVARIANTS (see ddpg_sweep.yaml): features [1024, 512, 256] + layer
# norm, batch 512 x 128 grad steps / 8k env steps (replay ratio ~8), buffer
# 500k, warmup 30k, gamma 0.99, tau 0.005, lr 3e-4.
#
# TWO CAVEATS when reading this arm:
#  1. mpo.py has no `n_step` argument -> 1-step returns (same gap as SAC).
#  2. COST: every grad step evaluates the critic on `num_action_samples` (20)
#     actions per state, so at batch 512 that is ~10k critic forwards per step,
#     ~20x the other arms. Held at learning_steps 128 anyway so the replay ratio
#     stays comparable — expect a much lower steps/s and judge it on
#     score-vs-env-steps first, wall-clock second. Drop learning_steps (and say
#     so) if it is too slow to finish alongside the rest.
name: "mpo"

args:
  gamma: 0.99
  tau: 0.005
  actor_learning_rate: 3e-4
  critic_learning_rate: 3e-4
  dual_learning_rate: 1e-2
  num_action_samples: 20          # actions sampled per state for the E-step
  epsilon: 0.1                    # E-step temperature KL bound
  epsilon_mean: 1e-3              # M-step KL bound on the policy mean
  epsilon_stddev: 1e-5            # M-step KL bound on the policy covariance
  init_temperature: 1.0
  init_alpha_mean: 1.0
  init_alpha_stddev: 1.0
  memory_warmup: 30_000
  steps_before_learning: 30_000
  steps_between_updates: 8_000
  learning_steps: 128
  max_grad_norm: 1

actor:
  # Layer norm is ON here (the library mpo.yaml leaves it off) to match the rest
  # of the sweep — architecture is meant to be the controlled variable.
  _target_: roxie.models.actors.StochasticActor
  features: [1024, 512, 256]
  use_layer_norm: true
  std_min: 1e-4
  std_max: 5.0

critic:
  _target_: roxie.models.critics.QCritic
  features: [1024, 512, 256]
  use_layer_norm: true
  dropout_rate: 0.

memory:
  _target_: flashbax.buffers.make_flat_buffer
  max_length: 500_000
  min_length: 10_000
  sample_batch_size: 512
  add_sequences: False
  add_batch_size: ${env.parallel_envs}
